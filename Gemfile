source "https://rubygems.org"

# GitHub Pages builds this site with the github-pages gem.
# Run locally with: bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
